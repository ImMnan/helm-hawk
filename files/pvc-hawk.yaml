apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hawk-shared-pvc
  namespace: hawk
spec:
  storageClassName: standard-rwx
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi